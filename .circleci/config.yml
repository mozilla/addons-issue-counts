version: 2
jobs:
  getIssues:
    steps:
      - checkout
      - run: npm install
      - run: npm run get-stats

workflows:
  version: 2
  hourly:
    triggers:
      - schedule:
          cron: "10 * * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - getIssues
